const StatusBadge = ({isOpenToWork}) => {
  const statusText = isOpenToWork ? "Open to Work" : "Busy learning"
  const statusColor = isOpenToWork ? "green"
  return (

  )
}
export default StatusBagde;